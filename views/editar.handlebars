<p class="text-start fs-5">Atualizar o agendamento do pet</p>
<p>
    <form action="/atualizar" method="POST">
        <input type="hidden" class="form-control" id="id" name="id" value="{{data.id}}">
        <div class="mb-3 text-start">
            <label class="form-label">Nome do pet:</label>
            <input type="text" name="nome" class="form-control" id="nome" value="{{data.nome}}">
        </div>
        <div class="mb-3 text-start">
            <label class="form-label">Telefone:</label>
            <input type="text" name="telefone" class="form-control" id="telefone" placeholder="(00)00000-0000" value="{{data.telefone}}">
        </div>
        <div class="mb-3 text-start">
            <label class="form-label">Serviços:</label>
            <select class="form-select" aria-label="Default select example" id="origem" name="origem">
                <option value="banho"{{#if ( eq data.origem "banho")}}selected{{/if}}>Banho</option>
                <option value="tosa" {{#if ( eq data.origem "tosa")}}selected{{/if}}>Tosa</option>
                <option value="banhoetosa" {{#if (eq data.origem "banhoetosa")}}selected{{/if}}>Banho e Tosa</option>
            </select>
        </div>
        <div class="mb-3 text-start">
            <label class="form-label">Data do agendamento:</label>
            <input type="date" name="data_contato" class="form-control" id="data_contato" aria-describedby="emailHelp" value="{{data.data_contato}}">
        </div>
        <div class="mb-3 text-start">
            <label class="form-label">Observação:</label>
            <textarea name="observacao" class="form-control" placeholder="" id="observacao" style="height: 100px">{{data.observacao}}</textarea>
        </div>
        <div class="mb-3 text-start">
            <button type="submit" class="btn btn-dark">Salvar</button>
        </div>
    </form>
</p>
        
